log_level: debug
state_output: mixed

smtp.from: "Salt <vagrant@localhost>"
smtp.to: "Vagrant <vagrant@localhost>"
smtp.host: "localhost"
smtp.subject: "Salt report:"
smtp.fields: "id,fun"

file_roots:
  base:
    - /srv/salt/states/base

pillar_roots:
  base:
    - /srv/salt/pillars/base
  private:
    - /srv/salt/vagrant/salt/pillars/private

reactor:
  - 'minion_start':
    - /srv/salt/reactor/sync_grains.sls
  - 'salt/minion/*/start':
    - /srv/salt/reactor/highstate_run.sls

extension_modules: /srv/salt/modules

schedule:
  package_upgrades:
    function: custom.execute
    hours: 6
    args:
      - '*'
      - 'pkg.list_upgrades'
