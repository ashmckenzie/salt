log_level: info
state_output: mixed

smtp.from: "Salt <ash@the-rebellion.net>"
smtp.to: "Ash McKenzie <ash@the-rebellion.net>"
smtp.host: "localhost"
smtp.subject: "Salt report:"
smtp.fields: "id,fun"

file_roots:
  base:
    - /srv/salt/states/base

pillar_roots:
  base:
    - /srv/salt/pillars/base
  private:
    - /srv/salt/pillars/private

reactor:
  - 'minion_start':
    - /srv/salt/reactor/sync_grains.sls
  - 'salt/minion/*/start':
    - /srv/salt/reactor/highstate_run.sls

extension_modules: /srv/salt/modules

schedule:
  package_upgrades:
    function: custom.execute
    hours: 6
    args:
      - '*'
      - 'pkg.list_upgrades'
